# GraphQL Characters Query

This project contains GraphQL queries to fetch specific characters by ID.

Each query retrieves the following fields:
- id
- name
- status
- species
- type
- gender

Characters queried: 1, 2, 3, 4
